// {
//     pages.map(
//       p => this.props.currentPage === p ? <span className="selectedPage"
//       onClick = {(e) => {this.onPageChanged(p);}}>{p}</span> :
//       <span onClick = {() => this.onPageChanged(p)}>{p}</span>
//       )

//     }



//     { pages.map( p => {
//         return <span className={this.props.currentPage ===  p && "selectedPage" }
//         onClick={(e) => { this.onPageChanged(p); }}>{p}</span>
//     })}


//from telegram

    // { 
    //     pages.map( (p)=> <span className={this.props.currentPage ===p ? "selectedPage": null}>{p}</span>)
        
        
    //     }



    //     map(p=> <span className={props.currentPage ===p? 'selectedPage': null}>{p}</span>)
